import React = require('react');
import Test from './Test';

// export default ({ name }) => <h1>Hello {name}!</h1>;

export default () => {
  return (
    <div>
      <h1>FUNC</h1>
    </div>
  );
};
